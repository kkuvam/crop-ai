# Core Data Science & ML

pandas==2.3.3
numpy==2.3.4
# xgboost (not installed, keep as is)
# scikit-learn (not installed, keep as is)
# joblib (not installed, keep as is)
# pyarrow (not installed, keep as is)
# statsmodels (not installed, keep as is)

# Web Scraping & HTTP
requests==2.32.5
beautifulsoup4==4.14.2
selenium==4.37.0
webdriver-manager==4.0.2
lxml==6.0.2

# Weather API
openmeteo-requests==1.7.3
openmeteo-sdk==1.21.2
requests-cache==1.2.1
retry-requests==2.0.0

# Jupyter & Notebooks
jupyter==1.1.1
jupyterlab==4.4.9
ipykernel==7.0.1
ipython==9.6.0
notebook==7.4.7
ipywidgets==8.1.7

# Data Formats
# openpyxl (not installed, keep as is)

# Visualization (optional, likely already included)
# matplotlib (not installed, keep as is)
# seaborn (not installed, keep as is)

# Utilities
python-dateutil==2.9.0.post0
python-dotenv==1.1.1
pytz>=2024.1
